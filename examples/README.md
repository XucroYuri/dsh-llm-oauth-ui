# Examples

## demo.sh

Shows available OAuth flows and demonstrates the login prompt.

```bash
./examples/demo.sh
```

Requires a DSH profile with `@deepseek-ai/dsh-authorization` mounted before `llm-pi-ai`.
